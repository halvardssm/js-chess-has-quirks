<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="stylesheets/splash.css" />
    <title>Chess has Quirks</title>
  </head>
  <body>
    <div id="header">
      <h1 class="title">Chess has Quirks</h1>
    </div>
    <div id="root">
      <div class="highscore">
        <h3>
          <%= gamesInitialized %> <% if (gamesInitialized != 1) { %> games <% }
          else { %> game <% } %> initialized, <%= gamesCompleted %> completed
        </h3>
      </div>
      <div class="button-wrapper">
        <form action="/play" method="get">
          <button type="submit">Play Now</button>
        </form>
      </div>
    </div>
  </body>
</html>
